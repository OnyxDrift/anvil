---
description: Show the state of the anvil vault — domain/subdomain tree, note counts, size, and a rough token estimate.
---
Run this exact command and show me the output, unedited:

    __ANVIL_HOME__/bin/status

This is read-only — it never writes or changes anything. After showing
the output, you can comment on anything that stands out (a domain-drift
warning, a domain with far more notes than others, a subdomain that
probably deserves its own domain), but don't repeat the numbers back to
me in prose — the command output already shows them.

$ARGUMENTS
