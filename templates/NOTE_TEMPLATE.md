---
type: procedural          # semantic | procedural | strategic | episodic
domain: software           # must match a line in anvil/domains.txt — broad and stable
subdomain:                 # normally a tool, technology, or language (e.g. syncthing, python, ssh) — NOT one of your own projects, use `project` for that. For a non-software domain (e.g. trading), may instead be a topical sub-category (e.g. backtesting, portfolio-theory)
project:                   # one of your own named projects (e.g. anvil, citeforge, swing-stack) — NOT a tool or technology, use `subdomain` for that
source: claude             # claude | grok | opencode | human
origin:                    # what backs the claim, not who wrote it: a git remote URL/repo path for code, a book/webpage/PDF for anything else
status: canonical          # draft | canonical | disputed | archived
updated: YYYY-MM-DD
tags: []                   # free-form keywords for search (e.g. [python, cli, docker])
---
# One-line claim title

Body: the claim and why. No transcript dump. Keep under ~400 words.
